getgenv().DarvkKey = ''

getgenv().accessoryCollision = false  -- Renamed from canCollide
getgenv().shoeCollision = false     -- Added separate shoe collision toggle
getgenv().anchored = false
getgenv().autoReload = false
getgenv().enableRefresh = false
getgenv().refreshKeybind = Enum.KeyCode.LeftAlt
getgenv().autoReloadWaitTime = 20
getgenv().Time = 1
getgenv().combineAccessories = true
getgenv().loadingMode = 'ItemsLoader' -- Options: "OutfitLoader", "ItemsLoader", "CombineBoth"
getgenv().targetPlayerUsername = '' -- Replace with the actual username
getgenv().FaceID = '' -- Actual face ID, now as a number
getgenv().Head = {} -- Accessories that go on the head or face
getgenv().Shoulder = {} -- Shoulder Accessories
getgenv().Torso = {} -- Accessories that go on the torso
getgenv().Waist = {} -- Anythng that goes on hips
getgenv().R6Blacklist = {} -- Replace with actual layered clothing IDs, also works with all other accessory id's
getgenv().Shoes = {} -- Put the id's of roblox shoes in here.
getgenv().Headless = false -- Gives the player headless
getgenv().showFaceWhenHeadless = false  -- Toggle for showing face when headless is enabled
getgenv().showHats = true  -- Toggle to show/hide hats
getgenv().showFace = true -- Hides your face
getgenv().removeAllItems = true -- Global variable using getgenv to toggle removing all items
getgenv().keepOriginalFace = true -- Keeps original face instead of replacing it with the faceId you chose

getgenv().scaleMode = 'Default'  -- Initialize scale mode first
getgenv().scalePresets = {
    ['Default'] = {
        autoScale = false,
        scaleMultiplier = 1,
        accessoryCategories = {
            Hat = 1.0, Hair = 1.0, Face = 1.0, Neck = 1.0,
            Shoulder = 1.0, Front = 1.0, Back = 1.0, Waist = 1.0,
            LeftShoe = 1.0, RightShoe = 1.0, LeftArm = 1.0, RightArm = 1.0,
            LeftLeg = 1.0, RightLeg = 1.0, Generic = 1.0
        }
    },
    ['Test'] = {
        autoScale = false,
        scaleMultiplier = 1,
        accessoryCategories = {
            Hat = 1.0, Hair = 1.0, Face = 1.0, Neck = 0.5,
            Shoulder = 1.0, Front = 1.0, Back = 0.7, Waist = 1.0,
            LeftShoe = 1.0, RightShoe = 1.0, LeftArm = 1.0, RightArm = 1.0,
            LeftLeg = 1.0, RightLeg = 1.0, Generic = 1.0
        }
    }
}

loadstring(game:HttpGet(('https://raw.githubusercontent.com/Cxltures/zig/refs/heads/main/API%20(Unoptimized%20Encrypted)')))()
